CMake Qt5 GUI Template
----------------------

You need Qt5 and CMake 2.8.11 or higher

-- Build instructions --
Where BUILD_TYPE := Debug|RelWithDebInfo|Release
      INSTALL := install base path, i.e. "/usr" | "/usr/local"

cd /your/project/build
cmake -DCMAKE_BUILD_TYPE=BUILD_TYPE -DCMAKE_INSTALL_PREFIX=INSTALL ../
make


CMake Options:
    -DCMAKE_VERBOSE_MAKEFILE:BOOL=ON

To run local build:
./filters

-- NOTE --
To run locally, the application UI .rc file must be copied into the
kxmlgui5 path, i.e.:

sudo cp ../src/APPui.rc /usr/local/share/kxmlgui5/filters/

- To install in user directory:
make install/fast

- To install in system directory:
sudo make install/fast


-- Tutorials and resources --

Qt online docs
https://doc.qt.io/
